{{#content}}
  <div class="header-wrapper">
    {{#header}}
      <header class="header-main container-fluid" role="banner">
        <div class="toplogo"><a href="{{url}}" class="{{class}}" target="_blank">{{label}}</a></div>
        <h1 class="sitename"><a href="{{../../appUrl}}/">{{../../appName}}</a></h1>
      </header>
    {{/header}}
  </div>
  {{>nav}}
  {{>warning}}
{{/content}}
