<!doctype html>
<html lang="en"  class="nojs">
<head>{{>head}}</head>
<body id="{{id}}" data-app="{{appUrl}}" data-appRoot="{{appRoot}}">
  {{>header}}
  <main class="main container-fluid" role="main"  id="mainContent" tabindex="-1">
    {{#content}}
      {{#top}}
        <h1 class="page-title">{{title}}</h1>
      {{/top}}
      {{#items}}
      <div class="items-widget">
        <div class="top">
          <div class="resultsnum">Showing  <span class="start"></span> - <span class="docslength"></span> of <span class="numfound"></span> results for <span class="s-query"></span></div>
          <div class="filters">
              {{#features}}
                <select id="browse-select" aria-label="Sort Books">
                {{#each filters}}
                  <option data-label-active="{{labelActive}}" data-label-inactive="{{labelInactive}}" data-sort-dir="{{direction}}" value="{{field}}" {{#if selected}}selected{{/if}}>{{#if selected}}{{labelActive}}{{else}}{{labelInactive}}{{/if}}</option>
                {{/each}}
              {{/features}}
            </select>
          </div>
        </div>
        <div id="items" class="widget items" data-name="items" data-rows="{{rows}}" data-source="{{datasource}}" data-fl="{{fl}}" {{#fq}}data-fq-{{filter}}="{{value}}"{{/fq}}></div>
        <div class="bottom text-center">
          <div id="paginator" class="pagination"></div>
        </div>
      </div>
      {{/items}}
    {{/content}}
  </main>
  {{>footer}}
  {{{closure}}}
</body>
</html>
